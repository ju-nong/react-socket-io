{
    "version": 2,
    "public": true,
    "builds": [{ "src": "package.json", "use": "@vercel/node" }],
    "routes": [{ "src": "/api/(.*)", "dest": "/api/index.js" }],
    "headers": [
        {
            "source": "/(.*)"
        }
    ]
}
